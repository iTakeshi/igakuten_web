<li <%= 'class=disabled' if current_page.first? %>>
  <% if current_page.first? %>
    <span>
  <% end %>
  <%= link_to_unless current_page.first?, '新しい投稿', url, :rel => 'prev', :remote => remote %>
  <% if current_page.first? %>
    </span>
  <% end %>
</li>
